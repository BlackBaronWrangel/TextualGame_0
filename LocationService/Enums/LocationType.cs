
namespace GlobalServices.Enums
{
    public enum LocationType
    {
        Default,
        Custom,
        OpenWorld //Location is part of default map with free navigation (game automatically builds navigation connections). Processed every game state.
    }
}
